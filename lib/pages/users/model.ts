// I know we have same model under the api/users/model, but better to keep them sapartely.
export interface UserDetail {
  id: number;
  name: string;
  username: string;
  email: string;
  website: string;
}
